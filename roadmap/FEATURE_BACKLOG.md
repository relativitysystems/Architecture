# Feature Backlog

Source repositories: `relativitysystems/AIKB` and `relativitysystems/Relativity`. Every item below is derived directly from evidence found during the architecture review that produced this documentation set (dead code, explicit TODO comments, documented security gaps, and schema/code already built but not yet wired up). No item here is speculative product ideation — see `roadmap/MASTER_ROADMAP.md` for that instead.

**Exception:** the [Go-to-Market Priority](#go-to-market-priority) section below is explicitly *not* code-evidence-derived technical debt — it is a business/product deliverable, included here per direct product direction because it is currently the highest-priority item on the roadmap. Everything else in this document keeps its original code-evidence-only scope.

## Overview

This backlog groups technical work into High/Medium/Low priority based on risk and impact observed directly in the code: security gaps affecting tenant isolation or credential handling are High; inconsistencies and unfinished-but-scaffolded features are Medium; dead code and minor cleanup are Low. Each item cross-references the architecture document where the underlying evidence is documented in full.

## Current Implementation

The items below reference the current state of both codebases as documented in [AIKB.md](../architecture/AIKB.md), [INGESTION_PIPELINE.md](../architecture/INGESTION_PIPELINE.md), [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md), [SECURITY.md](../architecture/SECURITY.md), [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md), [KNOWLEDGE_ANALYTICS.md](../product/KNOWLEDGE_ANALYTICS.md), and [KNOWLEDGE_GAP_DETECTION.md](../product/KNOWLEDGE_GAP_DETECTION.md). This document does not repeat that detail — it only lists the actionable follow-up each finding implies.

## Architecture

No new architecture is proposed by this document itself; every item below either closes a gap in the existing architecture or completes a feature the existing schema/code already anticipates (e.g., a column or table that exists but has no reader/writer yet).

---

## Go-to-Market Priority

| # | Item | Notes |
|---|---|---|
| GTM1 | **Demo Video and Sales-Ready Demo Account.** Choose the demo customer scenario; prepare realistic business documents; configure General and Slack knowledge collections; verify portal queries; verify Slack questions; prepare a short demo script; record the product walkthrough; add narration explaining pain point, value proposition, and differentiation; edit and review the video; publish it to the appropriate website/outreach location; prepare a shorter cut for direct outreach if useful. Treat this as a product and go-to-market deliverable, not merely content creation — see [../roadmap/MASTER_ROADMAP.md](MASTER_ROADMAP.md)'s dedicated Demo Video milestone for acceptance criteria and the dependency checklist, and [../go-to-market/DEMO_VIDEO_STRATEGY.md](../go-to-market/DEMO_VIDEO_STRATEGY.md) for the full narrative strategy, fictional-company storyline, feature-by-feature script framework, and production checklists. | Depends only on Slack bounded-delivery and Knowledge Collections being verified in staging (both are code-complete; see H5 below and [MASTER_ROADMAP.md](MASTER_ROADMAP.md) Track A) — does **not** wait for any other item in this backlog, including the High-priority security items below. |

---

## High Priority

Security-relevant gaps with a direct tenant-isolation or credential-handling impact.

| # | Item | Evidence |
|---|---|---|
| H2 | ✅ **Completed.** ~~Migrate Google Drive and Dropbox OAuth tokens off the legacy plaintext `oauth_tokens` table onto the already-built encrypted `oauth_connections`/`oauth_credentials` model~~ — a direct query confirmed `oauth_tokens` held zero live rows for any provider, so this was a code-path migration, not a live-credential migration with breakage risk. `routes/auth.js`'s dropbox/google callbacks now call `oauthConnectionsService.createOrReplaceConnection`; `getClientConnectionStatus`/`getAllClientsWithStatus` read all three providers from `oauth_connections` uniformly. Added `updateCredentialForConnection()` (in-place credential refresh, no connection-row churn) since Google's token needs silent background refresh and Slack's never did — `googleDriveService.js#getValidAccessToken` uses it, correctly preserving the existing refresh token when Google's grant response omits a new one. `migrations/20260719_gdrive_dropbox_oauth_connections.sql` cleans up the confirmed-empty legacy rows — applied to the Global Supabase project and verified via direct query (0 rows for either provider). 4 new tests added; 270/270 Relativity tests passing. | [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md), [SECURITY.md](../architecture/SECURITY.md), [ADR-006](../decisions/ADR-006-OAUTH-CREDENTIAL-ENCRYPTION.md) |
| H3 | ✅ **Completed.** ~~Replace non-constant-time secret comparisons with `crypto.timingSafeEqual`~~ — shared API key checks now constant-time in both repos (`Relativity/middleware/apiKey.js`, `aikb/routes/knowledge.js`), along with the admin session-signature check (`Relativity/middleware/adminAuth.js`) and the admin password check (`Relativity/routes/admin.js#POST /login`). All comparisons follow the existing length-check-then-`timingSafeEqual` pattern already used by `slackSignatureService.js`/`serviceRequestAuth.js`. 266/266 Relativity and 47/47 AIKB tests passing. | [SECURITY.md](../architecture/SECURITY.md) — Current Risks |
| H4 | ✅ **Completed — 14 of 14 routes.** ~~Add per-caller entitlement verification to AIKB's `x-api-key`-only routes~~ — extended the existing additive HMAC-signed envelope (`requireServiceRequest`, already used by `POST /ask`, see [ADR-004](../decisions/ADR-004-SIGNED-SERVICE-REQUESTS.md)) to `POST /ingest`, `POST /reindex`, `DELETE /document/:id`, `DELETE /client/:clientId`, `GET /documents/:clientId`, `GET /collections/:clientId`, `POST /collections`, `PATCH /collections/:id`, `DELETE /collections/:id`, `PATCH /document/:id/collection`, and — in a second pass closing out this item's previously-residual scope — `GET /jobs/:clientId`, `GET /summary/:clientId`, `GET /analytics/:clientId`, and `GET /stats/:clientId`. The envelope binds `clientId` cryptographically, so a leaked shared `x-api-key` alone can no longer act on an arbitrary client through any of these 14 routes. The four reporting routes reuse the GET-with-signed-JSON-body pattern already established for `/documents/:clientId` and `/collections/:clientId` (no middleware change needed — `express.json()` parses `req.body` regardless of verb) and the same URL-param-vs-envelope `clientId` cross-check (400 on mismatch) used by every other `clientId`-in-path route. Verified via a live smoke test against a real client (`Relativity Systems`) proving, for all four routes: a correctly signed envelope succeeds (200), a missing envelope 401s, a wrong-secret signature 401s, a tampered payload 401s, and a mismatched URL-vs-envelope `clientId` 400s. Relativity's `services/aikbService.js` (`listIngestionJobs`, `getClientSummary`, `getClientAnalytics`, `getClientKnowledgeStats`) now signs and sends the envelope as a JSON body on these GET requests, alongside the unchanged `x-api-key`. Both repos updated together in the same session to avoid a window where one side expects the envelope and the other doesn't send it, but actual deploy-ordering in production is outside this repo's visibility. 47/47 AIKB and 270/270 Relativity tests passing. | [AIKB.md](../architecture/AIKB.md) — Current Limitations, [SECURITY.md](../architecture/SECURITY.md), [ADR-004](../decisions/ADR-004-SIGNED-SERVICE-REQUESTS.md) |
| H5 | ✅ **Completed.** ~~Slack Terminal Delivery Failure Handling.~~ Bounded, immediate-in-flow Slack delivery retries implemented (3 total attempts by default, configurable); on exhaustion, the `slack_event_log` row is marked with a terminal `delivery_failed` status, the stored question is redacted, and only dedup/diagnostic metadata is retained. The `GET /api/integrations/slack/sweep` scheduled-recovery path has been **removed**, not merely disabled. AIKB-side chat content is redacted via a best-effort signed callback. 266/266 Relativity and 47/47 AIKB tests passing. See [ADR-007](../decisions/ADR-007-SLACK-BOUNDED-DELIVERY-RETRY.md)'s Implementation Status section for full detail, including the follow-ups tracked as M14 below. | [ADR-007](../decisions/ADR-007-SLACK-BOUNDED-DELIVERY-RETRY.md), [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md), [../history/ARCHITECTURE_REVIEW_PHASES.md](../history/ARCHITECTURE_REVIEW_PHASES.md) |

## Medium Priority

Inconsistencies between providers, and features whose schema/backend already exists but whose UI or automatic behavior does not.

| # | Item | Evidence |
|---|---|---|
| M1 | ✅ **Completed.** ~~Backport the hashed, server-stored OAuth-state pattern (`oauthStateService`) from Slack to Google Drive and Dropbox, replacing their unsigned base64 `state` parameter~~ — `oauth_states` (migration `20260715_oauth_states.sql`) already supported `google_drive`/`dropbox` as valid providers (added for future use, never wired up until now), so no schema change was needed. `routes/auth.js`'s `/dropbox/start`, `/dropbox/callback`, `/google/start`, `/google/callback` now call `oauthStateService.generateAndStoreState`/`consumeState` exactly like Slack's flow, replacing the unsigned `base64(JSON.stringify({clientId}))` state — which trusted a client-supplied `clientId` with nothing binding it to the session — with a hashed, server-stored, single-use, 10-minute-TTL token. `connectedByMemberId` is now populated on both providers' `createOrReplaceConnection` calls (previously always `null`, since the old state carried no `memberId`). Google's callback no longer echoes `clientId` in its redirect URL (`portal.js` never read it). Verified via a live smoke test proving a fresh state proceeds to token exchange, a reused state is rejected, a missing/denied/bogus state redirects safely, and `/start` still 401s without a valid session. 280/280 Relativity tests passing. | [SECURITY.md](../architecture/SECURITY.md), [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md) |
| M2 | ✅ **Completed.** ~~Hash team-invite tokens at rest~~ — `team_invites` gets a new `token_hash` column (SHA-256 hex, matching `oauthStateService.js#hashState`'s algorithm/encoding) via a migration (`Relativity/supabase/migrations/20260719_hash_team_invite_tokens.sql`) that backfilled the 2 existing rows via `pgcrypto` and then dropped the plaintext `token` column entirely. `services/supabaseService.js` (`createTeamInvite`, `getTeamInviteByToken`, `acceptTeamInvite`, `regenerateTeamInvite`) reads/writes only `token_hash`, hashing in application code before every query — the raw token generation/emailing (`routes/team.js#generateToken`) is unchanged. **Migration applied to production and verified via direct query**: `token` column confirmed gone, `token_hash` confirmed `NOT NULL` with a unique index (`uq_team_invites_token_hash`), row count unchanged (2/2) confirming the backfill preserved every existing invite. 280/280 Relativity tests passing. | [SECURITY.md](../architecture/SECURITY.md) |
| M3 | ✅ **Completed — no migration required.** ~~Add key-rotation support for `INTEGRATION_CREDENTIAL_ENCRYPTION_KEY`~~ — the `encryption_key_version` column already existed on `oauth_credentials`, so this was a pure application-layer fix. `services/integrationCredentialEncryption.js` gained `getCurrentKeyVersion()` (reads the new, optional `INTEGRATION_CREDENTIAL_ENCRYPTION_KEY_VERSION` env var, defaulting to `1` so every pre-existing environment needs zero config changes) and a version-aware `decryptCredential(envelope, keyVersion)`. This also fixed a real latent bug: `getDecryptedCredentialForConnection` previously read `encryption_key_version` off each row but never actually passed it to `decryptCredential`, always decrypting with whatever key happened to be "current" — silently broken the moment a rotation ever happened. `oauthConnectionsService.js` gained `listConnectionIdsNeedingKeyRotation()` and `reencryptCredentialForConnection()` (decrypt under the row's old version, re-encrypt under the current one), plus a manual operator script (`scripts/rotateCredentialEncryptionKey.js`) that drives a full rotation to completion. Rotation model documented in `.env.example`: keep the old key as `INTEGRATION_CREDENTIAL_ENCRYPTION_KEY_V{oldVersion}` while re-encrypting, then remove it. 9 new tests added (rotation round-trip, retired-key-removed error case, no-op-when-already-current); 280/280 Relativity tests passing. | [SECURITY.md](../architecture/SECURITY.md) |
| M4 | ✅ **Completed.** ~~Implement automatic, deduplicated knowledge-gap persistence server-side, using the `idempotency_key` column and unique index already added in migration 005 but not yet used by any write path~~ — `services/runKnowledgeQuery.js` now calls `createKnowledgeGap` itself on both gap-detection branches (`no_chunks_found`, `answer_indicated_not_found`), for both `origin: 'portal'` and `origin: 'slack'` — a deliberate reversal of the prior "only `POST /api/knowledge/gaps` ever writes a gap" invariant (the test asserting the old behavior, `test/runKnowledgeQuery.test.js`, was rewritten to assert the new one). Dedup key derivation is a new shared helper, `services/knowledgeGapKey.js` (`buildGapIdempotencyKey`): `gap:v1:<clientId>:<sha256(normalized question)>:<ISO week>` — week-bucketed so a recurring unresolved question resurfaces roughly weekly instead of staying silently open forever, while repeats within the same week collapse onto one row. `services/supabaseService.js#createKnowledgeGap` now upserts on `idempotency_key` (insert-on-conflict-do-nothing, then a follow-up update of `reason`/`session_id`/`message_id` only — `status` and `reported_by` are never touched by a conflict-path write, so a recurring question never resets an admin's review state). The manual `POST /api/knowledge/gaps` save now derives and passes the same key, so it converges onto an already-auto-detected row instead of duplicating it. Best-effort: a `createKnowledgeGap` failure is logged and swallowed, never breaks the user-facing answer/Slack reply. 9 new/rewritten tests in `aikb/test/runKnowledgeQuery.test.js` plus a new `aikb/test/knowledgeGapKey.test.js` (8 tests); 54/54 AIKB tests passing. | [KNOWLEDGE_GAP_DETECTION.md](../product/KNOWLEDGE_GAP_DETECTION.md) |
| M5 | ✅ **Completed.** ~~Build an admin review workflow for knowledge gaps using the existing `status` lifecycle (`open`/`reviewed`/`resolved`/`ignored`) — the column exists but no code currently reads or writes it~~ — added `GET /api/knowledge/gaps/:clientId` (status-filterable list) and `PATCH /api/knowledge/gaps/:id` (status transition) to `aikb/routes/knowledge.js`, both behind `requireServiceRequest` (same signed-envelope pattern as the H4 collections routes), backed by three new `supabaseService.js` functions (`listKnowledgeGapsByClient`, `getKnowledgeGapById`, `updateKnowledgeGapStatus`). Relativity: two new proxy functions in `services/aikbService.js` (`listKnowledgeGaps`, `updateKnowledgeGapStatus`), a fan-out `GET /admin/gaps` and `PATCH /admin/gaps/:clientId/:gapId` in `routes/admin.js` (same per-client `Promise.allSettled` fan-out pattern as `/analytics`/aikb-health, since AIKB's envelope is signed per-client), and a new "Knowledge Gaps" admin tab (`public/admin/admin.html`/`admin.js`) — a status-filterable table with a client column and an inline status `<select>` per row, following the existing Issues tab's list/render/inline-update pattern exactly, lazy-loaded on first tab visit like CRM (since it fans out across every client, unlike Issues' single Relativity-DB query). No new Relativity-side tests were added for the admin routes/proxy functions — consistent with this repo's existing convention of not unit-testing admin.js's other thin per-client fan-out routes (e.g. `/analytics`, `/clients/:clientId/aikb-health`) or aikbService.js's simpler proxy functions (e.g. `listCollections`) either; verified via the full 280/280 Relativity + 54/54 AIKB suites passing plus manual code-path review. | [KNOWLEDGE_GAP_DETECTION.md](../product/KNOWLEDGE_GAP_DETECTION.md) |
| M6 | ✅ **Completed.** ~~Add general-purpose rate limiting (portal login, admin login, AIKB's `x-api-key`-gated routes) and an explicit CORS policy~~ — added `express-rate-limit` and `cors` to both repos. **Relativity:** an explicit CORS allowlist (`middleware/corsPolicy.js`, `ALLOWED_ORIGINS` env var, defaults to `APP_BASE_URL` only) ahead of every route; `routes/admin.js#POST /login` (10 req/15min), and `routes/team.js#GET /team/invites/verify` + `routes/auth.js#POST /complete-invite` (30 req/15min, shared limiter — same unauthenticated-token-guessing threat model) via `middleware/rateLimiters.js`. **True portal login (`public/portal/login.js#signInWithPassword`) never reaches this server** — the browser calls Supabase Auth directly with the anon key — so it is enforced by Supabase's own platform-level rate limiting, not by this repository; documented as such rather than silently left uncovered. **AIKB:** `middleware/rateLimit.js` applies a generous 2000 req/15min cap across all of `/api/knowledge` (defense-in-depth against a leaked key, not a per-tenant quota), and `middleware/corsPolicy.js` sets an explicit `origin: false` (AIKB is never called from a browser, so this documents that as a decision rather than an accidental gap). Verified via a live smoke test of both servers: CORS headers present/absent correctly for allowed/disallowed origins, 429s returned after the configured limit, rate-limit headers present. A disallowed-origin request was found to throw an unhandled 500 with a stack trace on the first pass — fixed to `callback(null, false)` (deny without erroring) before landing. 280/280 Relativity and 47/47 AIKB tests passing. | [SECURITY.md](../architecture/SECURITY.md) |
| M7 | ✅ **Completed.** ~~Implement the cross-client question-count rollup for the admin console — explicitly marked `// TODO: totalQuestions — needs a dedicated AIKB analytics endpoint` in `Relativity/routes/admin.js`~~ — the TODO's premise turned out to be outdated: no new AIKB endpoint was needed, since AIKB already computes per-client `totalQuestions` (`getClientSummaryData`, already used elsewhere, e.g. `/clients/:clientId/aikb-health`). `GET /admin/analytics` now also fans out `aikbService.getClientSummary(c.id)` per client (same `Promise.allSettled`-and-accumulate pattern as the existing `totalDocuments`/`totalIndexed`/`totalFailed` loop) and sums `totalQuestions`, returning `null` (not a wrong partial sum) if any client's summary call fails. **Caveat:** `GET /admin/analytics` itself has no frontend consumer anywhere in the repo today (confirmed via full-repo search) — the `.analytics-todo` CSS class referenced in `admin.css` is similarly unused. This item's literal scope (fixing the TODO) is done; surfacing it as a visible dashboard tile would be new frontend work with no existing endpoint-to-UI wiring to extend, and wasn't assumed. | [KNOWLEDGE_ANALYTICS.md](../product/KNOWLEDGE_ANALYTICS.md) |
| M8 | ✅ **Completed.** ~~Surface Google Drive/Dropbox connection status in the portal UI — `GET /auth/me` already returns `googleDriveConnected`/`dropboxConnected`, but neither is read anywhere in `portal.js`~~ — both flags are now destructured from `/auth/me`'s response in `portal.js` and rendered via two new integration cards in the existing "Integrations" panel (`portal.html`, copied from Slack's existing card markup), each with a status badge and an owner/admin-only Connect button wired to the existing `/auth/google/start`/`/auth/dropbox/start` routes (same redirect-on-`{url}` pattern as Slack's connect button). No disconnect button — neither provider has a disconnect endpoint today, so none was implied. | [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md) |
| M9 | ✅ **Completed.** ~~Add a UI control for the existing chat-session-rename endpoint (`PATCH /api/knowledge/chat/sessions/:id/title`) — implemented on the backend, unreachable from the frontend~~ — added a rename button (✎) next to each session's existing delete button in the chat-history list (`portal.js#renderSessions`), using a `prompt()`-based rename (consistent with delete's existing `confirm()`-based interaction, rather than introducing a new modal component for a single-field edit) that calls the already-proxied `PATCH /api/knowledge/chat/sessions/:sessionId/title` and updates the in-memory session list on success. | [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md) |
| M10 | ✅ **Completed.** ~~Extend collection-based retrieval filtering to the portal's own chat query — collections currently scope only Slack's retrieval, not in-portal chat~~ — AIKB needed no changes: `POST /api/knowledge/query` already accepted and threaded through `allowedCollectionIds`, only Relativity's proxy layer never passed one. `routes/api.js`'s `POST /knowledge/query` now accepts an optional `collectionIds` array from the request body and forwards it as `allowedCollectionIds` via `services/aikbService.js#queryKnowledge`'s new optional parameter. Portal frontend: a new "Search scope" collapsible control in the Knowledge Base tab (`portal.html`/`portal.js`), following the same checkbox-list pattern as Slack's existing collection allow-list UI, backed by `GET /api/collections`; selection persists per-member in `localStorage` (portal chat scoping is per-user, unlike Slack's client-wide allow-list) and is included as `collectionIds` on every chat send. The control stays hidden when a client has zero or only its single default collection, so accounts that never created a second collection see no behavior or UI change. | [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md), [AIKB.md](../architecture/AIKB.md) |
| M11 | ✅ **Verified already satisfied — no changes needed.** ~~Add Row-Level Security as defense-in-depth on the highest-sensitivity tables (`oauth_tokens`, `oauth_credentials`, `knowledge_chunks`)~~ — direct live-database inspection (`pg_tables`/`pg_policies`, Supabase's own `rls_enabled_no_policy` advisor) found RLS is **already enabled on all 3 named tables — and in fact on every table in both Supabase projects** (16 of 16 in Global, 7 of 7 in AIKB), evidently via a Supabase project-level default applied outside of tracked migration `.sql` text, which is why a prior grep-based audit concluded RLS was entirely absent. Existing application-layer checks are untouched, satisfying this item's stated constraint exactly. **Caveat carried forward, not closed by this item:** zero policies exist on any table, and every Supabase client in both repos uses the service-role key (`BYPASSRLS`), so RLS being "on" provides no practical backstop today — see [docs/supabase/global/RLS.md](../docs/supabase/global/RLS.md) and [docs/supabase/aikb/RLS.md](../docs/supabase/aikb/RLS.md) (both pre-existing audits, now re-verified and updated) for the full detail and the distinction from a real non-bypassing-role policy, which would be materially more work and wasn't what this item asked for. | [SECURITY.md](../architecture/SECURITY.md), [../docs/supabase/global/RLS.md](../docs/supabase/global/RLS.md), [../docs/supabase/aikb/RLS.md](../docs/supabase/aikb/RLS.md) |
| M12 | ✅ **Completed — shipped alongside M4/M5.** ~~Ship Milestone 6's fuller knowledge-gap/conversation-metadata polish — a system-vs-user `reportedBy` distinction and a richer `originMetadata` shape, on top of Milestone 4's already-shipped minimal `origin`/`origin_metadata`/`idempotency_key` schema slice~~ — new migration `aikb/migrations/008_gap_reported_by.sql` adds `knowledge_gaps.reported_by TEXT CHECK (reported_by IN ('system','user'))`, nullable with no default (the ~280 pre-existing rows get `NULL`/"legacy," never a guessed value, since guessing would misattribute rows this migration has no way to actually know the origin of). `createKnowledgeGap` now sets `reported_by: 'system'` for pipeline-detected gaps and `'user'` for manual saves. Richer `originMetadata`: Slack's flow already attached `{teamId, channelId, threadTs, eventId}` (unchanged); portal chat now attaches `{route: '/api/knowledge/query', memberRole}` (previously sent nothing) — the concrete context actually available at that call site. **Not yet applied to production** (by explicit choice, not oversight — see the migration file's own header note): the code that writes `reported_by` must not be deployed until `008` is applied, or inserts will fail with "column reported_by does not exist." | [../history/ARCHITECTURE_REVIEW_PHASES.md](../history/ARCHITECTURE_REVIEW_PHASES.md), [KNOWLEDGE_GAP_DETECTION.md](../product/KNOWLEDGE_GAP_DETECTION.md) |
| M13 | ✅ **Completed — rescoped to workspace-level authorization (employee-level authorization descoped), then revised: Slack conversations are no longer persisted in Supabase at all.** ~~Implement Milestone 7 — Slack direct-message support and per-user/employee-level authorization (Slack user → Relativity member identity mapping), explicitly deferred from the Slack MVP~~ — an earlier investigation (see history below) found full employee-level authorization was a substantial, multi-week new subsystem (OAuth re-consent rollout, a new Slack-user↔member mapping table, admin/self-serve linking UI, and new per-user authorization checks). That scope was deliberately dropped: **any user in a connected Slack workspace may DM the bot**, with no Slack-user-to-`client_members` mapping, no email verification, no admin linking UI, and no per-member collection permissions — DMs get exactly the same authorization channel `@mentions` already have, the client-wide `slack_collection_access` allow-list, reused unchanged. Employee-level authorization remains explicitly out of scope, not merely deferred. Original implementation: `slackEventsService.js#processEventCallback`'s event gate accepts both `app_mention` and `message`/`im`-channel-type events with identical downstream handling (dedup, `extractQuestion`, the client-wide collection allow-list, AIKB's ask flow, bounded delivery retries, terminal-failure handling); `message`/`mpim` (group DMs) remains explicitly unsupported and is safely dropped. <br><br>**Revision (this change):** the original implementation still created a `knowledge_chat_sessions` row (tagged `origin: 'slack'`/`'slack_dm'`) and `knowledge_chat_messages` rows for every Slack question, merely *excluding* them from portal-facing reads via a `.or('origin.is.null,origin.neq.slack_dm')` filter in `listChatSessions`/`getChatSession` — the conversation content was still fully persisted, just hidden. Product decision: Slack-originated conversations (both channel `@mentions` and DMs) must never be persisted at all, not hidden-but-stored. `services/runKnowledgeQuery.js` now takes a `persistConversation` flag (default `true`, unchanged for the portal); the Slack ask path (`inngest/functions.js#slackQuestionRequested`) calls it with `persistConversation: false`, so **no** `knowledge_chat_sessions`/`knowledge_chat_messages` row is ever created for Slack. Knowledge-gap logging is a deliberate, narrower exception: a detected gap still auto-persists a `knowledge_gaps` row with the real question text (product decision — gap review-queue data was never portal-exposed and isn't chat/session content). Removing the old session-based idempotency short-circuit required a replacement dedup mechanism: a new AIKB table, `knowledge_slack_request_log` (migration `009_slack_request_log.sql` — `client_id`, `idempotency_key` UNIQUE, `origin`, `status`, `attempt_count`, `error_category`, timestamps only, **never** question/answer/citation text), claimed by `routes/knowledge.js` `POST /ask` before enqueueing to Inngest, so a retried `/ask` call (Relativity's own in-flow `retryWithBackoff`) never triggers a second LLM call or a second Slack reply. On the Relativity side, `slack_event_log`'s `question` column (the one place that stored raw extracted question text, redacted only on terminal `delivery_failed`) is dropped entirely (migration `20260720_slack_event_log_drop_question.sql`) — it was written on every event but never read back by any code path. AIKB's `/chat/redact` callback and `slackDeliveryFailureService`'s AIKB-redact call remain in place (harmless no-ops for new Slack events, since there is no longer any session to redact) for defense in depth against any residual pre-migration state. Existing historical Slack-origin content was purged from production as part of this change: 14 `knowledge_chat_sessions` rows (12 `slack` + 2 `slack_dm`) and their 28 `knowledge_chat_messages` were hard-deleted from AIKB; `slack_event_log`'s 15 existing `question` values were removed by the column drop. Pre-Milestone-4 sessions with `origin IS NULL` were untouched and remain visible in the portal, as before. **Explicitly flagged, not addressed by this revision:** Inngest's own event/step-output store still carries the question (event payload) and answer/sources (the `run-knowledge-query` step's memoized return value) through its own persistence layer, which is separate from Supabase and out of this change's stated scope ("never persisted in Supabase"). Test coverage: `aikb/test/runKnowledgeQuery.test.js` (persistConversation: false paths), `aikb/test/slackRequestLogService.test.js` (claim/dedup), `Relativity/test/slackEventLogService.test.js` and `Relativity/test/slackEventsService.test.js` (question-field removal). | [../history/ARCHITECTURE_REVIEW_PHASES.md](../history/ARCHITECTURE_REVIEW_PHASES.md), [CONNECTOR_ROADMAP.md](CONNECTOR_ROADMAP.md) |
| M14 | **Slack delivery-failure operational follow-ups (H5 completed; these were not part of its scope).** (a) Manual staging/production-like verification of the bounded-retry/`delivery_failed` flow, since automated tests cover logic but not a live Slack workspace/portal walkthrough — see [MASTER_ROADMAP.md](MASTER_ROADMAP.md) Track A. (b) Monitoring/alerting for a sustained rise in `delivery_failed` events, currently visible only in application logs. (c) Observability for AIKB redaction-callback failures — the callback is best-effort and its failures are only logged, never surfaced. (d) Implement the technical-metadata retention/cleanup TODO left in `Relativity/services/slackEventLogService.js` (ADR-007 suggested 7–30 days; no mechanism exists in either repository yet). None of these block the demo video (GTM1) or the Go-to-Market track. | [ADR-007](../decisions/ADR-007-SLACK-BOUNDED-DELIVERY-RETRY.md) — Known Gaps, [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md) — Current Limitations |

## Low Priority

Dead code removal and minor consistency cleanup with no functional or security impact.

| # | Item | Evidence |
|---|---|---|
| L1 | ✅ **Completed.** ~~Remove or wire up `aikb/middleware/resolveContext.js`'s permissive `resolveContext` function~~ — confirmed unused anywhere in the repo and deleted; `requireMemberContext` (the function actually used by every route) is untouched. | [AIKB.md](../architecture/AIKB.md) |
| L2 | ✅ **Completed.** ~~Remove `Relativity/services/dropboxService.js#listFiles()` or wire it into a real route~~ — confirmed orphaned (its own comment referenced an `/api/dropbox/files/:clientId` route that doesn't exist in this repo) and deleted. | [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md) |
| L3 | ✅ **Completed.** ~~Remove the "chat welcome chips" dead code in the portal~~ — removed the dead `.chat-chip` click-handler block in `portal.js` (a no-op since the element was already commented out) and the corresponding commented-out HTML block in `portal.html`. | [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md) |
| L4 | ✅ **Completed.** ~~Remove leftover "TEMP DEBUG" `console.log`/`console.error` statements in the voice-transcription code path~~ — removed the debug upload-metadata log and trimmed the verbose error dump in `routes/api.js` down to the single-line `console.error(label, err.message)` style used everywhere else in that file; removed the equivalent debug log in `portal.js`. | [CLIENT_PORTAL.md](../product/CLIENT_PORTAL.md) |
| L5 | ✅ **Completed.** ~~Consolidate AIKB's two overlapping analytics endpoints (`/summary`, `/analytics`), which independently compute overlapping aggregates on every call with no shared computation or caching~~ — investigation found the duplication was actually three-way (`/summary`, `/analytics`, and `/jobs` all independently queried `knowledge_ingestion_jobs`, up to 4x for one client across the three routes; `knowledge_chat_messages`/`knowledge_gaps` were each queried identically 2x), and that Relativity's admin dashboard (`GET /admin/clients`, `GET /admin/clients/:clientId/aikb-health`) fired all three in parallel for every client on every page load — the actual real-world manifestation of the redundancy. Added `getClientKnowledgeStats`/`GET /api/knowledge/stats/:clientId` in `aikb`, computing each underlying table exactly once (backed by three new shared query helpers: `fetchRecentIngestionJobs`, `fetchUserQuestionTimestamps`, `fetchKnowledgeGapsCount`, also now used internally by `/summary` and `/analytics`); switched both admin routes in `Relativity/routes/admin.js` to call it instead of three separate functions. `/summary`, `/analytics`, `/jobs` and their Relativity wrappers are unchanged and still used standalone (portal calls `/analytics` directly; `/jobs` backs `routes/api.js`'s own `/knowledge/jobs`). Verified via a live smoke test against a real client that `/stats`'s output is a byte-for-byte union of the three old endpoints' output. 47/47 AIKB and 266/266 Relativity tests passing. | [KNOWLEDGE_ANALYTICS.md](../product/KNOWLEDGE_ANALYTICS.md) |
| L6 | ✅ **Completed.** ~~Clean up `deleteLegacyDocumentsForClient`'s reference to an untracked legacy `documents` table~~ — confirmed via direct query that the table held 0 rows with no write activity since the prior audit (`last_autoanalyze` unchanged), no incoming FKs, RLS enabled with zero policies. `deleteLegacyDocumentsForClient()` and its call site removed from `aikb/services/supabaseService.js`; `aikb/migrations/007_drop_legacy_documents.sql` dropped both `public.documents` and `match_documents()` together (never one without the other) — applied to production and verified gone via direct query (`information_schema.tables`/`pg_proc`, both zero rows). One caveat was accepted as a residual, unverifiable risk rather than a blocker: whether any consumer external to these two repos called `match_documents()` directly via PostgREST — that surface no longer exists either way, since the function is dropped. 47/47 AIKB tests passing. | [AIKB.md](../architecture/AIKB.md), [../docs/audits/SCHEMA_DRIFT.md](../docs/audits/SCHEMA_DRIFT.md) |
| L7 | ✅ **Completed.** ~~Delete AIKB's legacy `routes/slack.js` (`410` stub) and its unused `SLACK_BOT_TOKEN`/`SLACK_SIGNING_SECRET` config reads~~ — confirmed the file is not fully inert (still performs live Slack signature verification and answers the `url_verification` handshake, asserted by `test/slackEventsRetired.test.js`), so it was kept as-is. Only the genuinely-unused `botToken`/`SLACK_BOT_TOKEN` read was removed, from `aikb/config/index.js` and `.env.example`; `SLACK_SIGNING_SECRET` is untouched (still required). 47/47 AIKB tests passing. | [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md), [../history/ARCHITECTURE_REVIEW_PHASES.md](../history/ARCHITECTURE_REVIEW_PHASES.md) |
| L8 | ✅ **Completed.** ~~Document `SERVICE_REQUEST_SIGNING_SECRET`, `RELATIVITY_API_BASE_URL`, and `RELATIVITY_DELIVER_TIMEOUT_MS` in AIKB's `.env.example`~~ — all three added with brief comments matching the file's existing style. | [../history/ARCHITECTURE_REVIEW_PHASES.md](../history/ARCHITECTURE_REVIEW_PHASES.md) |
| L9 | ✅ **Completed. Reclassified from H1.** ~~Remove (or properly wire up) `GET /api/google-drive/files/:clientId` and `/file/:clientId/:fileId` in `Relativity/routes/api.js`~~ — re-confirmed nothing calls them anywhere in the repo (grepped both route paths, only the definitions themselves matched) and `API_KEY` is still configured nowhere, so they 401'd unconditionally regardless of caller. Deleted both routes outright, along with the now-unused `apiKey` middleware import in `routes/api.js` (the middleware module itself is untouched — still used by `routes/auth.js`). `googleDriveService.js`'s `getValidAccessToken`/`listFiles`/`downloadFile` were left in place (out of scope — no longer called from anywhere, but removing exported service functions wasn't part of the audited evidence for this item). 266/266 Relativity tests passing. | [SECURITY.md](../architecture/SECURITY.md) — Client Isolation Gaps |

---

## Current Limitations

This backlog (excluding the [Go-to-Market Priority](#go-to-market-priority) section, which is deliberately business-priority-driven, not evidence-derived) is scoped to what is directly evidenced in the two source codebases as of this review — it excludes further product ideation, new-integration proposals (Gmail/Outlook/Teams/CRM — see [CONNECTOR_FRAMEWORK.md](../architecture/CONNECTOR_FRAMEWORK.md) for the pattern those would follow, not a scheduled item here), and anything requiring information outside the codebase (e.g., customer feedback) to prioritize correctly. Priority levels for the H/M/L sections reflect risk/impact as inferred from code, not business priority, which may reorder these items — the Go-to-Market item is the one deliberate exception, and it currently outranks every H-priority item on business grounds. See [MASTER_ROADMAP.md](MASTER_ROADMAP.md) for the full rationale.

## Future Extension Points

As new architecture documents in this repository are written or updated, new evidence-based items should be added here in the same format (item, one-line description, cross-reference to the document containing the full evidence) rather than as a separate, disconnected backlog — keeping this document the single place technical debt and scaffolded-but-incomplete features are tracked.
